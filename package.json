{
  "name": "2025-gridchallenge",
  "version": "1.0.0",
  "description": "2025 Grid Challenge - Testing pan/zoom implementations",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:json": "playwright test --reporter=json > test-results.json",
    "spec": "node gen_spec.js > SPEC.md",
    "manual": "node generate_manual_test.js > manual_test.html",
    "repomix": "npm run spec && npm run test:json || true && repomix"
  },
  "keywords": [
    "grid",
    "testing",
    "playwright",
    "pan",
    "zoom"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
